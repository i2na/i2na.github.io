export interface BlogPost {
    slug: string;
    title: string;
    createdAt: string;
    summary: string;
    content: string;
    fileName: string;
}

export interface BlogPostMetadata {
    slug: string;
    title: string;
    createdAt: string;
    summary: string;
    fileName: string;
}
