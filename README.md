# Getting Started

## Prerequisites

-   Node.js

## Run Locally

1. Install dependencies:

    ```bash
    yarn install
    ```

2. Set `GEMINI_API_KEY` in `.env.local`

3. Run the app:
    ```bash
    yarn dev
    ```
