export interface Post {
  slug: string;
  title: string;
  createdAt: string;
  summary: string;
  content: string;
  fileName: string;
}

export interface PostMetadata {
  slug: string;
  title: string;
  createdAt: string;
  summary: string;
  fileName: string;
}
